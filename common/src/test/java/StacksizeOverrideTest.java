import com.lonkachu.stackable.StacksizeOverride;
import org.junit.jupiter.api.Test;

public class StacksizeOverrideTest {
    @Test
    public void TestResourceLocation()
    {
        String expectedNamespace = "minecraft:dirt";
        StacksizeOverride override = new StacksizeOverride("minecraft:dirt", Integer.MAX_VALUE);

        override.GetIdentifier().;
    }
}
